<!DOCTYPE html>
<html lang="sr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carisma - Rent a Car</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="hero-section">
        <div class="hero-content">
            <div class="logo-container">
                <h1 class="logo">Carisma</h1>
                <div class="logo-underline"></div>
            </div>

            <p class="tagline">Vaša pouzdana aplikacija za iznajmljivanje vozila</p>

            <div class="main-card">
                <h2>Jednostavno. Brzo. Pouzdano.</h2>
                <p>
                    Moderna web-aplikacija za brzo iznajmljivanje vozila.
                    Rezervišite vozilo u nekoliko klikova i krenite na put već danas.
                </p>
            </div>

            <div class="features-container">
                <a href="Identity/Account/Register" class="feature-item">
                    <i class="fas fa-user-plus"></i>
                    <h3>Registrujte se</h3>
                    <p>Započnite u 3 klika</p>
                </a>

                <a href="Podrska" class="feature-item">
                    <i class="fas fa-headset"></i>
                    <h3>24/7 Podrška</h3>
                    <p>Uvijek tu za vas</p>
                </a>
            </div>

            <a href="Vozilo" class="cta-button">
                <span>Počnite Vašu Vožnju</span>
                <i class="fas fa-arrow-right"></i>
            </a>
        </div>

        <div class="floating-shapes">
            <div class="shape shape-1"></div>
            <div class="shape shape-2"></div>
            <div class="shape shape-3"></div>
        </div>
    </div>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #ff6b9d 0%, #c44569 50%, #4a0e2d 100%);
            min-height: 100vh;
            overflow-x: hidden;
        }

        .hero-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 2rem;
        }

        .hero-content {
            max-width: 800px;
            text-align: center;
            color: white;
            z-index: 2;
            position: relative;
        }

        .logo-container {
            margin-bottom: 2rem;
        }

        .logo {
            font-size: 4rem;
            font-weight: 700;
            background: linear-gradient(45deg, #ff69b4, #ff1493);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
            letter-spacing: -2px;
        }

        .logo-underline {
            width: 60px;
            height: 4px;
            background: linear-gradient(45deg, #ff69b4, #ff1493);
            margin: 0 auto;
            border-radius: 2px;
            animation: slideIn 1s ease-out 0.5s both;
        }

        .tagline {
            font-size: 1.25rem;
            opacity: 0.9;
            margin-bottom: 3rem;
            font-weight: 300;
            animation: fadeInUp 1s ease-out 0.7s both;
            color: #ff69b4;
        }

        .main-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 192, 203, 0.3);
            border-radius: 24px;
            padding: 2.5rem;
            margin-bottom: 3rem;
            transition: all 0.3s ease;
            animation: fadeInUp 1s ease-out 0.9s both;
            box-shadow: 0 8px 32px rgba(255, 105, 180, 0.2);
        }

            .main-card:hover {
                transform: translateY(-5px);
                background: rgba(255, 255, 255, 0.98);
                box-shadow: 0 12px 40px rgba(255, 105, 180, 0.3);
            }

            .main-card h2 {
                font-size: 1.75rem;
                margin-bottom: 1rem;
                font-weight: 600;
                color: #c44569;
            }

            .main-card p {
                font-size: 1.1rem;
                line-height: 1.6;
                color: #8b4a6b;
            }

        .features-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
            animation: fadeInUp 1s ease-out 1.1s both;
        }

        .feature-item {
            background: rgba(255, 255, 255, 0.12);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 192, 203, 0.25);
            border-radius: 16px;
            padding: 1.5rem;
            text-decoration: none;
            color: white;
            transition: all 0.3s ease;
            display: block;
        }

            .feature-item h3 {
                font-size: 1.1rem;
                margin-bottom: 0.5rem;
                font-weight: 600;
                color: #ff69b4;
                transition: color 0.3s ease;
            }

            .feature-item p {
                font-size: 0.9rem;
                opacity: 0.9;
                color: #ff69b4;
                transition: color 0.3s ease;
            }

            .feature-item:hover {
                transform: translateY(-3px);
                background: rgba(255, 255, 255, 0.18);
                box-shadow: 0 10px 30px rgba(255, 105, 180, 0.3);
            }

                .feature-item:hover h3 {
                    color: #007bff;
                }

                .feature-item:hover p {
                    color: #007bff;
                }

            .feature-item i {
                font-size: 2rem;
                margin-bottom: 1rem;
                color: #ff69b4;
            }

        .cta-button {
            background: linear-gradient(45deg, #ff69b4, #ff1493, #dc143c);
            border: none;
            border-radius: 50px;
            padding: 1rem 2.5rem;
            font-size: 1.1rem;
            font-weight: 600;
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(255, 20, 147, 0.4);
            animation: fadeInUp 1s ease-out 1.3s both;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 0 auto;
            width: fit-content;
            text-decoration: none;
        }

            .cta-button:hover {
                transform: translateY(-2px) scale(1.05);
                box-shadow: 0 15px 40px rgba(255, 20, 147, 0.5);
            }

            .cta-button i {
                transition: transform 0.3s ease;
            }

            .cta-button:hover i {
                transform: translateX(3px);
            }

        .floating-shapes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }

        .shape {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 192, 203, 0.08);
            animation: float 6s ease-in-out infinite;
        }

        .shape-1 {
            width: 100px;
            height: 100px;
            top: 20%;
            left: 10%;
            animation-delay: 0s;
            background: rgba(255, 105, 180, 0.1);
        }

        .shape-2 {
            width: 150px;
            height: 150px;
            top: 60%;
            right: 15%;
            animation-delay: 2s;
            background: rgba(255, 20, 147, 0.08);
        }

        .shape-3 {
            width: 80px;
            height: 80px;
            bottom: 20%;
            left: 20%;
            animation-delay: 4s;
            background: rgba(255, 182, 193, 0.12);
        }

        @@keyframes float {
            0%, 100%

        {
            transform: translateY(0px) rotate(0deg);
        }

        50% {
            transform: translateY(-20px) rotate(180deg);
        }

        }

        @@keyframes fadeInUp {
            from

        {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        @@keyframes slideIn {
            from

        {
            width: 0;
        }

        to {
            width: 60px;
        }

        }

        @@media (max-width: 768px) {
            .logo

        {
            font-size: 2.5rem;
        }

        .tagline {
            font-size: 1.1rem;
        }

        .main-card {
            padding: 2rem;
            margin-bottom: 2rem;
        }

        .features-container {
            grid-template-columns: 1fr;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .hero-section {
            padding: 1rem;
        }

        .hero-content {
            max-width: 100%;
        }

        }

        @@media (max-width: 480px) {
            .logo

        {
            font-size: 2rem;
        }

        .main-card h2 {
            font-size: 1.5rem;
        }

        .main-card p {
            font-size: 1rem;
        }

        }
    </style>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            
            const featureItems = document.querySelectorAll('.feature-item');
            featureItems.forEach(item => {
                item.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-8px) scale(1.02)';
                });

                item.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0) scale(1)';
                });
            });

            
            const ctaButton = document.querySelector('.cta-button');
            ctaButton.addEventListener('click', function(e) {
         
                this.style.transform = 'scale(0.98)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 150);
            });

            
            window.addEventListener('scroll', function() {
                const scrolled = window.pageYOffset;
                const shapes = document.querySelectorAll('.shape');

                shapes.forEach((shape, index) => {
                    const speed = 0.1 + (index * 0.05);
                    shape.style.transform = `translateY(${scrolled * speed}px) rotate(${scrolled * 0.1}deg)`;
                });
            });
        });
    </script>
</body>
</html>